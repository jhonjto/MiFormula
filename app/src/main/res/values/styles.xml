<resources>

    <style name="CustomActionBarTheme"
        parent="@style/Theme.AppCompat.Light">
        <!--<item name="android:actionBarStyle">@style/MyActionBar</item>-->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/Teal</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/GreenStrong</item>
        <!-- colorAccent is used as the default value for colorControlActivated
         which is used to tint widgets -->
        <item name="colorAccent">@color/Beige</item>
        <!-- ActionBar title text -->
        <item name="actionBarStyle">@style/MyActionBarTitleText</item>


    </style>

    <!-- ActionBar title text -->
    <style name="MyActionBarTitleText" parent="@style/Widget.AppCompat.Light.ActionBar">
        <!--Background of the ActionBar-->
        <item name="background">@color/Teal</item>
        <!--Used color for the text ActionBar-->
        <item name="titleTextStyle">@style/Title</item>
        <!--Used color for the subtitle text ActionBar-->
        <item name="subtitleTextStyle">@style/Title</item>
    </style>

    <!--Define the color of the text-->
    <style name="Title" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/Beige</item>
    </style>

    <!--Define the style of the button-->
    <style name="button_text" >
        <item name="android:layout_width" >fill_parent</item>
        <item name="android:layout_height" >wrap_content</item>
        <item name="android:textColor" >#ffffff</item>
        <item name="android:gravity" >center</item>
        <item name="android:layout_margin" >3dp</item>
        <item name="android:textSize" >20sp</item>
        <item name="android:textStyle" >bold</item>
        <item name="android:shadowColor" >#4C9BD6</item>
        <item name="android:shadowDx" >1</item>
        <item name="android:shadowDy" >1</item>
        <item name="android:shadowRadius" >2</item>
    </style>

</resources>
